# Marble Run 弹珠轨道拼装游戏 — 分阶段计划

> 状态：已确认方案 A。Stage 0–3 已完成，Stage 4 第一批零件已完成（2026-09-05）。

## 0. 一句话目标

在浏览器里玩的 3D 弹珠轨道游戏：玩家从"零件库"里拿轨道模块（直道、弯道、漏斗、螺旋、跳台、电梯……）在网格上拼装，然后放弹珠进去看它跑，可以多颗弹珠比赛。

## 1. 技术选型（推荐方案 A）

| 方案 | 渲染 | 物理 | 优点 | 缺点 |
|---|---|---|---|---|
| **A. Three.js + Rapier3D（WASM）+ Vite + TypeScript** | Three.js | Rapier | 本机已有 Node 24，不用装东西；Rapier 是目前浏览器里最快的物理引擎，确定性好；能用 Playwright 自动截图测试；发布就是一个静态网页 | 编辑器 UI 要自己写（用 HTML 叠在画布上即可） |
| B. Babylon.js + Havok | Babylon | Havok | 官方一体化，物理很强 | 生态比 Three 小，我不如 Three 熟 |
| C. Godot 4 | Godot | Jolt | 自带场景编辑器、导出 Web/桌面 | 需要安装 Godot；迭代靠 GDScript 文件，我无法可视化操作编辑器，调试链路长 |
| D. 2D（Matter.js / Planck） | Canvas | 2D 引擎 | 最简单 | 弹珠轨道的核心乐趣是 3D 螺旋/漏斗/落差，2D 会很寡淡 |

**推荐 A。** 现在不需要你安装任何东西。后面如果要做精细美术模型，可选装 Blender（非必需，第一版轨道零件全部用代码程序化生成几何）。

### 关键技术决定

- **轨道零件用程序化几何**：每个零件 = 一条中心线曲线（直线 / 圆弧 / 螺旋线）+ 一个 U 形截面，扫出网格；同一份网格喂给 Rapier 做 trimesh 碰撞体。不依赖外部模型资源，随时改参数。
- **弹珠物理**：固定步长（1/120 s）、Rapier 的 CCD（连续碰撞）开着，防止高速穿模。
- **网格 + 接口（port）系统**：零件放在 1 单位的立方体格子上，每个零件声明若干"进/出口"（位置 + 方向 + 高度）。放置时接口吸附对齐，这是"能拼起来"的核心。
- **存档格式**：纯 JSON（零件类型、格子坐标、旋转、参数），可存 localStorage、导出文件、后期压缩进 URL 分享。
- **可测试**：从第一天起暴露 `window.__TEST__`（放零件 / 放弹珠 / 步进物理 / 读取弹珠位置），用 Playwright 跑回归 + 截图。

## 2. 阶段划分

每个阶段结束都有一个"能打开看的东西"。

### Stage 0 — 骨架（半天）✅ 完成

- Vite + TS + Three.js + Rapier 跑起来，固定步长物理循环，轨道相机（OrbitControls）。
- 一块斜坡 + 一颗弹珠滚下来，落到地面。
- 调试用：物理碰撞体线框开关、FPS。
- `window.__TEST__` 雏形 + 一个 Playwright 冒烟测试。

**验收**：打开网页，弹珠稳定滚下斜坡，不穿模、不抖。

### Stage 1 — 零件系统（1–2 天）✅ 完成（实际 12 种零件：起点/直道/斜道/陡坡/左右小弯/左右大弯/螺旋x1/螺旋x2/漏斗/终点）

- 零件定义框架：`PieceDef { id, 几何生成器, 碰撞体, ports[], 可旋转方向 }`。
- 首批 6 种基础零件：
  1. 直道（平）
  2. 斜道（下降 1 格）
  3. 90° 弯道
  4. 螺旋下降（1 圈降 2 格）
  5. 漏斗（多入口汇成一个出口）
  6. 起点发射台 / 终点碗
- 弹珠在任意手拼（先用代码拼）的组合轨道上能顺畅跑完。

**验收**：用代码写死一条 10 个零件的轨道，弹珠从头跑到尾。

### Stage 2 — 拼装编辑器（2–3 天）✅ 完成（零件栏带缩略图、幽灵预览、接口吸附 + R 切换方案、空接口黄点标记、Q/E 换层、右键删除、撤销重做、localStorage 自动存档、导出/导入 JSON、编辑/试玩切换）

- 左侧零件栏，点选 → 鼠标在网格上出现半透明预览 → 点击放置；R 旋转、Delete 删除。
- 接口吸附：预览时自动对齐相邻零件的出口，能接上时高亮绿色。
- 撤销 / 重做，保存 / 加载（localStorage + 导出 JSON 文件）。
- 编辑 / 试玩两个模式切换，试玩模式下放弹珠。

**验收**：完全用鼠标拼一条轨道，存档，刷新后加载，弹珠能跑。

### Stage 3 — 玩法与手感（2 天）✅ 完成（单发/8 连发/定时连发、比赛计时与排名面板、跟随相机、慢动作、Web Audio 合成的滚动声与碰撞声、玻璃纹理弹珠；编辑器另做了接龙模式/选中编辑/相机改造）

- 弹珠生成：单颗 / 一排 / 定时连发；多颗不同颜色。
- 跟随相机（锁定某颗弹珠）、慢动作、重置。
- 终点检测 → 比赛模式：排名榜、计时。
- 音效：弹珠碰撞、滚动声（Web Audio 合成或 CC0 音效），随速度变化。
- 基础视觉：阴影、简单材质、天空背景。

**验收**：拼一条赛道，放 8 颗弹珠比赛，出排名，看着爽。

### Stage 4 — 更多模型类型（持续）🔶 第一批完成：机关框架（运动学/动态刚体 + 每步更新）、交替分叉器（翻板）、Y 形合流、5x5 漩涡碗（切向进碗绕圈）、跷跷板（铰链 + 配重）

分批加，按"有趣程度 / 实现成本"排：

- **机关类**：跷跷板、弹簧跳台、翻板、旋转风车、闸门（一颗过去关一次）。
- **动力类**：螺旋提升机 / 电梯（把弹珠送回高处，实现循环轨道）。
- **分流类**：随机分叉器、Y 形合流、计数分流（每 3 颗换一边）。
- **表演类**：木琴音阶道（经过发声）、多米诺、环形跳跃（loop）。
- **可调参数零件**：长度 / 坡度 / 弯道半径 可拖动调整。

**验收**：每加一个零件都附带一个演示存档和一条 Playwright 用例。

### Stage 5 — 打磨与分享

- 预置关卡 / 示例轨道 5 条。
- URL 分享（JSON 压缩进 hash）。
- 手机端触控操作。
- 视觉升级（可选 Blender 做零件外观替换程序化网格，接口不变）。
- 部署到 GitHub Pages / Netlify（推送和部署前会先问你确认）。

## 3. 参考项目（可借鉴代码或思路）

- [yomboprime/marbleMachine](https://github.com/yomboprime/marbleMachine) — Three.js 弹珠机物理演示，MIT。
- [Zylann/marbles](https://github.com/Zylann/marbles) — Godot 的方块式拼轨道沙盒（Game Jam 作品），MIT，思路可参考。
- [dvdjrnx/marble-race](https://github.com/dvdjrnx/marble-race)、[hazem3500/r3f-marble-game](https://github.com/hazem3500/r3f-marble-game) — R3F 弹珠竞速，看相机跟随和手感。
- [viridia/demo-rapier-three](https://github.com/viridia/demo-rapier-three) — Three + Rapier 最小集成示例。
- [dimforge/rapier](https://github.com/dimforge/rapier) — 物理引擎本体。
- 商业对标：Marble World、Marble It Up! Ultra、Rolling Line（火车但拼装逻辑同类）。

## 4. 需要你拍板的事

1. **3D 还是 2D？** 我推荐 3D（方案 A）。
2. **美术风格**：低多边形玩具木质感（我推荐，程序化几何好看且省事）/ 塑料彩色 / 科幻。
3. **侧重点**：偏"拼装沙盒看着爽"，还是偏"多弹珠比赛竞速"？影响 Stage 3 的优先级。
4. **发布目标**：只本机玩 / 挂到网上分享。

## 5. 目录结构预想

```
marble/
  index.html
  src/
    main.ts            入口，游戏循环
    physics/           Rapier 封装，固定步长，CCD
    pieces/            每种零件一个文件 + registry
    geometry/          中心线扫描生成 U 形轨道网格
    editor/            网格、吸附、放置、撤销
    game/              弹珠生成、比赛、终点
    ui/                HTML 叠层 UI
    test-seam.ts       window.__TEST__
  tests/               Playwright
  levels/              示例存档 JSON
```
